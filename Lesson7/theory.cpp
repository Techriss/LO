#include <iostream>
#include <cmath>
using namespace std;

int x, w;

int main()
{
    pow(x, w);  // x^w
    sqrt(x);    // √x
    abs(x);     // |x|

    sin(x);
    cos(x);
    tan(x);

    ceil(x);
    floor(x);
    round(x);

    M_PI;       // π
}